{
  "name": "@skulptur/workspace",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "lerna run --scope=@skulptur/library-fns build && lerna run --scope @skulptur/app dev",
    "build": "lerna run --parallel build",
    "lib:build": "lerna run --scope=@skulptur/library-fns build",
    "pretify": "prettier --write ."
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "license": "MIT"
}
